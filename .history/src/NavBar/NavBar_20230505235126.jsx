import styled from 'styled-components'

const StyledNavBarWrapper = styled.header``

const NavBar = () => {
  return (
    <StyledNavBarWrapper>
      <nav></nav>
    </header>
  )
}

export default NavBar
