import styled from 'styled-components'

const StyledNavBarWrapper = styled.header``

const NavBar = () => {
  return (
    <StyledNavBarWrapper>
      <nav></nav>
    </StyledNavBarWrapper>
  )
}

export default NavBar
