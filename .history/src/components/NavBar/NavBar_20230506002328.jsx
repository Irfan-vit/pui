import styled from 'styled-components'

const StyledNavBarWrapper = styled.header``

const StyledNav

const NavBar = () => {
  return (
    <StyledNavBarWrapper>
      <nav></nav>
    </StyledNavBarWrapper>
  )
}

export default NavBar
