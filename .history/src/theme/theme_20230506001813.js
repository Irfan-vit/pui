import defaultColors from './defaultThemeColors'
// import typography from './typogarphy'
export const defaultTheme = {
  primaryBg: defaultColors.primaryBg,
  offSetBg: defaultColors.offSetBg,
  textOnPrimary: defaultColors.textOnPrimary,
  textOnOffSet: defaultColors.textOnOffSet,
  action: defaultColors.action,

  textColor: colors.textColor,
  textMuted: colors.textMuted,
}
export const darkTheme = {
  primaryColor: colors.textOnPrimary,
  textOnPrimary: colors.primaryHeavy,
  primaryBackground: colors.darkColor,
  textColor: colors.textColor,
  textMuted: colors.textMuted,
  footer: colors.footer,
}
